name: cronicadeportiva
description: Crónica deportiva app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dartx: ^0.8.0
  dartz: ^0.10.0
  flutter_hooks: ^0.18.1
  flutter_modular: ^4.2.0
  freezed_annotation: ^1.0.0
  flutter_snake_navigationbar: ^0.5.0
  intl: ^0.17.0
  json_annotation: ^4.3.0
  logging: ^1.0.2
  logging_appenders: ^1.0.0
  path: ^1.8.0
  riverpod_context: ^0.1.1
  rxdart: ^0.27.3
  rx_shared_preferences: ^2.2.0
  timeago_flutter: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^3.1.0
  build_runner: ^2.1.5
  flutter_lints: ^1.0.0
  freezed: ^1.0.0
  json_serializable: ^6.0.1

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/legal/